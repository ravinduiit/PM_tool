// Import Bootstrap CSS
import 'bootstrap/dist/css/bootstrap.min.css';

// Import Bootstrap JavaScript
import 'bootstrap';

// Optionally import your custom CSS (if you have any)
import '../css/app.css';

// Import DataTables JavaScript (needed for DataTables functionality)
import 'datatables.net';
import 'datatables.net-bs5';  // For Bootstrap 5 styling of DataTables


// Optionally import jQuery (if not already bundled with Bootstrap or other dependencies)
import $ from 'jquery';


// Import Laravel's default bootstrap (optional, depending on your setup)
import './bootstrap';
